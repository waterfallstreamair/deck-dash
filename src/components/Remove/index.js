import styled from 'styled-components';

const Remove = styled.div`
  font-size: 1em;
  color: #cccccc;
  position: absolute;
  top: 0em;
  width: 18em;
  text-align: right;
`;

export default Remove;
